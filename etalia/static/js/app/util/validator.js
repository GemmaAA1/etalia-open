define('validator', ['underscore'], function(_) {

    var Validator = function() {

    };

    Validator.prototype.validateModel = function(object, constraints) {

    };

    Validator.prototype.validateValue = function(value, constraints) {

    };

    return Validator;
});
